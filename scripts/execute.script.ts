
const { ethers, upgrades } = require("hardhat");

async function executeScript() {
    
}

async function main() {
  await executeScript();
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
